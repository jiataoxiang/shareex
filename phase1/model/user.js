class User {
    constructor(user_id, name, password, email) {
        this.user_id = user_id
        this.name = name
        this.password = password
        this.email = email
        this.posts = [
            
        ]
    }
}

export default User;

class Jason extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default Jason;